# 字幕组任务管理 Bot 设计文档

## 一、项目背景

### 1.1 团队现状

字幕组日常工作以"集"为单位推进，每集需经过多道工序协作完成。目前任务分配和进度跟踪依赖人工在腾讯文档智能表中手动填写，存在信息滞后、漏接、重复接活等问题。

### 1.2 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 数据存储 | 腾讯文档智能表 | 每个番剧一个子表，表名格式：`番剧名_工作表` |
| 群聊平台 | QQ 群 | 每个番剧绑定一个工作群，另设总群查看全局进度 |
| Bot 框架 | NoneBot2 + NapCat | NapCat 提供 QQ 协议适配，NoneBot2 处理命令逻辑 |
| 文档 API | 腾讯文档开放平台 Open API | 个人开发者可用，OAuth2.0 授权，每日 2000 次调用额度 |

### 1.3 API 额度约束

腾讯文档开放平台个人开发者每日限额 **2000 次 API 调用**。设计中需注意：

- Bot 启动时拉取一次全量数据缓存至内存，后续查询优先读缓存
- 写操作（接活、完成等）实时调用 API，同时同步更新本地缓存
- 查询类命令（`/进度`、`/我的任务`、`/待接`）全部走本地缓存，不消耗 API 额度
- 设置定时全量同步（如每 30 分钟一次），防止缓存与远端漂移
- 预估日常消耗：新建集 ×1~2 次（每次约 10~20 条写入）+ 接活/完成 ×20~30 次 + 定时同步 ×48 次 ≈ 200~300 次/天，远低于 2000 上限

> **多 key 轮换池（D18）**：2000/天按"开发者应用/账号"计。若日后番剧数 / 同步频率上来逼近上限，可在 `SUBFLOW_TENCENT_DOC_KEYS` 里配多套**已各自授权同一批文档**的凭据（不同开发者账号），storage 按每次调用 round-robin 轮换，有效额度叠到约 N×2000；某把 key 的 token 失效或触发限流时自动转移到下一把。前提是每个账号都被授权访问同一批智能表（同账号多签 token 不叠加配额）。

### 1.4 设计原则：模块化与可迁移

本插件未来可能迁移至飞书多维表格、或从 NoneBot2 迁移至 AstrBot 等其他框架。因此架构上严格分层，各层通过接口（抽象类）解耦：

```
┌─────────────────────────────────────────────┐
│             commands（命令解析层）             │ ← 依赖 Bot 框架（NoneBot2 / AstrBot）
├─────────────────────────────────────────────┤
│           task_manager（业务逻辑层）           │ ← 纯业务，不依赖任何框架或存储实现
├─────────────────────────────────────────────┤
│            storage（存储抽象层）               │ ← 抽象接口
│  ┌──────────────┐  ┌──────────────────────┐ │
│  │ TencentDoc   │  │ FeishuSheet（未来）   │ │ ← 具体实现，可替换
│  └──────────────┘  └──────────────────────┘ │
├─────────────────────────────────────────────┤
│           messenger（通知抽象层）              │ ← 抽象接口
│  ┌──────────────┐  ┌──────────────────────┐ │
│  │ QQMessenger  │  │ FeishuMessenger      │ │ ← 具体实现，可替换
│  └──────────────┘  └──────────────────────┘ │
└─────────────────────────────────────────────┘
```

迁移时只需替换最底层的实现，业务逻辑层和命令层不受影响。

---

## 二、智能表结构设计

### 2.1 子表规则

每个番剧对应一个子表，命名格式：`{番剧名}_工作表`

示例：`淡岛百景_工作表`、`孤独摇滚_工作表`

### 2.2 番剧与 QQ 群绑定

#### 群类型

| 群类型 | 说明 | 命令权限 | 通知推送 |
|--------|------|----------|----------|
| **工作群** | 每个番剧绑定的专属群 | 可执行所有命令（接活/完成/新建等） | 接收该番剧的所有通知（任务创建、工序推进等） |
| **总群** | 全局配置一个，所有番剧共享 | 可执行只读查询命令（`/进度`、`/我的任务`、`/待接` 查看所有番剧） | **不接收任何推送通知** |

设计要点：
- 总群通过配置项 `main_group_id` 指定，不占用绑定关系
- 在总群发送 `/进度 淡岛百景 07` 可查看任何番剧，无需绑定
- 在总群发送 `/接活` 等写操作命令会被拒绝，提示"请到对应工作群操作"
- 一个工作群可以绑定多个番剧，但一个番剧只能绑定一个工作群

#### 绑定方式

支持两种绑定方式，适配不同场景：

**方式一：按番剧名绑定**（智能表子表名符合 `{番剧名}_工作表` 命名规范时）

```
/绑定 淡岛百景
```

Bot 自动查找腾讯文档中名为 `淡岛百景_工作表` 的子表并关联。

**方式二：按 ID 绑定**（子表命名不规范、或需要指定特定文档时）

```
/绑定id <fileID> <sheetID> [别名]
```

直接通过腾讯文档的 fileID 和 sheetID 绑定，可选设置一个别名用于后续命令中引用：

```
/绑定id abc123 sheet456 淡岛百景
```

后续命令中使用别名即可：`/接活 淡岛百景 07 翻译 P1`

#### 绑定数据结构

```json
{
  "main_group_id": 111111111,
  "bindings": {
    "淡岛百景": {
      "group_id": 123456789,
      "file_id": "abc123",
      "sheet_id": "smartsheet_xxx",
      "bound_by": 987654321,
      "bound_at": "2026-05-25T14:00:00"
    },
    "孤独摇滚": {
      "group_id": 223456789,
      "file_id": "abc123",
      "sheet_id": "smartsheet_yyy",
      "bound_by": 987654321,
      "bound_at": "2026-05-20T10:00:00"
    }
  }
}
```

### 2.3 表头字段

| 字段名 | 类型 | 说明 | 示例值 |
|--------|------|------|--------|
| 类型 | 单选 | 工序类型 | 翻译 / 时轴 / 校对 / 后期 / 监制 / 压制 |
| 集数 | 文本 | 集数编号，从项目名中独立出来便于查询过滤 | `07`、`OVA1`、`OP`、`ED` |
| 分段 | 文本 | 数字串：分段工序填 `1`/`2`/`3`...；不分段的工序填 `0`（D12） | `1`、`2`、`0` |
| 组员 | 文本 | 接活人标识（Bot 写入 QQ 号；手动填表可能是昵称） | `123456789` 或 `小明` |
| 进度 | 单选 | 当前任务状态 | 未分配 / 已分配 / 进行中 / 已完成 / 归档 |
| 完成时间 | 日期 | 标记完成时自动写入 | `2026-05-25` |
| 备注 | 文本 | 自由备注 | `加急`、`需要配合第6集` |

### 2.4 组员字段与通知的兼容策略

由于需要兼容手动填表，组员字段的值可能是 QQ 号，也可能是昵称或其他文本。Bot 在发送通知时按以下逻辑处理：

```
读取组员字段值
  │
  ├─ 纯数字且能匹配到群成员 QQ 号 → @该成员（CQ at）
  │
  └─ 非纯数字 / 数字但不在群内 → 作为文本显示原值
```

示例：
- 组员字段为 `123456789`（群内成员） → `@小明 完成了 翻译 P1 ✅`
- 组员字段为 `小明`（手动填的昵称） → `小明 完成了 翻译 P1 ✅`
- 下游通知同理：`校对现在可以接了！` 如果校对员已接活且组员是 QQ 号，则 @；否则发文本

> D16：所有 @ 都是 OneBot 的 CQ 艾特码（真正艾特到人），不是字面量文本。除接活/完成/放弃等通知外，`/进度` 进度看板、`/我的任务` 抬头、`/删除任务` 确认摘要里的组员同样以艾特码呈现。

### 2.5 为什么拆"集数"和"分段"

原始设计中"项目"字段承载了番剧名+集数+分段信息（如 `淡岛百景07（0-8）`），格式不统一，Bot 解析困难。拆分后：

- **番剧名**已由子表名承载，无需重复
- **集数**独立为字段，支持 API 按集数精确过滤
- **分段**独立为字段，便于判断"该工序是否需要分段""某集的所有分段是否完成"

---

## 三、工序流水线（可自定义）

### 3.1 设计思路

不同番剧的工序流程可能不同：有的翻译分段时轴不分段，有的没有后期环节，有的校对之前只需要翻译不需要时轴。因此工序流水线不硬编码，而是**按番剧配置**。

每个番剧的流水线配置包含：

```json
{
  "淡岛百景": {
    "pipeline": [
      {
        "stage": "翻译",
        "segment": true,
        "depends_on": []
      },
      {
        "stage": "时轴",
        "segment": true,
        "depends_on": []
      },
      {
        "stage": "校对",
        "segment": false,
        "depends_on": ["翻译", "时轴"]
      },
      {
        "stage": "后期",
        "segment": false,
        "depends_on": ["校对"]
      },
      {
        "stage": "监制",
        "segment": false,
        "depends_on": ["后期"]
      },
      {
        "stage": "压制",
        "segment": false,
        "depends_on": ["监制"]
      }
    ]
  }
}
```

每个工序节点有三个属性：
- `stage`：工序名称
- `segment`：是否按时间段分段（true = 新建集时按 1/2/3/... 展开，false = 只生成一条「分段=0」记录）（D12）
- `depends_on`：前置工序列表（空 = 无依赖，可立即接活）

### 3.2 典型流水线模板

**标准流程**（翻译+时轴均分段）：

```
翻译（分段） ──┐
               ├──→ 校对 → 后期 → 监制 → 压制
时轴（分段） ──┘
```

**简化流程**（无后期）：

```
翻译（分段） ──┐
               ├──→ 校对 → 监制 → 压制
时轴（不分段）──┘
```

**最简流程**（单人翻译+校对）：

```
翻译（不分段）→ 校对 → 压制
```

### 3.3 流水线管理命令

#### `/设置流水线 <番剧名> <流水线定义>`

用简洁的 DSL 语法定义工序链：

```
/设置流水线 淡岛百景 翻译[分段] → 时轴[分段] → 校对 → 后期 → 监制 → 压制
```

语法规则：
- 逗号分隔的工序**可并行**，共同作为下一步的前置依赖
- `→` 表示严格串行
- `[分段]` 标记该工序需要按时间段展开，不标记则默认不分段
- 可随时修改，修改后只影响新建的集，已有任务不受影响

#### `/查看流水线 <番剧名>`

查看当前配置的工序链。

#### 预设模板

首次绑定番剧时，如未设置流水线，使用默认模板：

```
翻译[分段] → 时轴[分段] → 校对 → 后期 → 监制 → 压制
```

### 3.4 依赖检查逻辑

当某人执行 `/完成` 时，Bot 根据该番剧的流水线配置执行依赖检查：

```
0. 前置校验（D15）：当前任务的前置工序是否都已完成？
   （同段串行时按同段，混合情况按全段；详见 3.2 / D13）
   ├─ 否 → 拒绝完成，回复"前置任务未完成，无法完成：['翻译 2']"
   └─ 是 → 继续步骤 1
1. 更新当前任务进度 → "已完成"，写入完成时间
2. 查询同集、同工序的所有分段 → 是否全部完成？
   ├─ 否 → 仅回复"已标记完成，该工序还有 N 个分段未完成"
   └─ 是 → 继续步骤 3
3. 遍历流水线，找到所有 depends_on 包含当前工序的下游工序
4. 对每个下游工序，检查其 depends_on 中的所有工序是否都已全部完成
   ├─ 全部满足 → 群内通知"XX 现在可以接了！"
   └─ 部分满足 → 回复"XX 已全部完成，等待 YY 完成后 ZZ 即可开始"
```

> **D15**：前置依赖只在 `/完成` 时校验，`/接活` 不校验——组员可以提前认领排队的任务，等上游就绪再完成。

### 3.5 特殊集类型

- **OP/ED 等特殊任务**：通过 `/新建特殊` 手动指定需要的工序类型（见 4.1 节），不受流水线模板约束
- **特别篇/OVA**：集数填 `OVA1`、`SP1` 等，使用番剧的标准流水线

---

## 四、命令体系

所有命令均有**群作用域**：

- **工作群**：Bot 收到命令时，通过 `group_index` 查询当前群绑定了哪些番剧，命令中的番剧名必须在绑定列表内，否则回复"该番剧未绑定到本群"
- **总群**：仅允许查询类命令（`/进度`、`/我的任务`、`/待接`、`/绑定列表`），可查看所有番剧的数据；写操作命令会被拒绝并提示"请到对应工作群操作"

### 4.1 管理员命令

#### `/绑定 <番剧名>`

将当前群与指定番剧绑定。Bot 自动查找腾讯文档中名为 `{番剧名}_工作表` 的子表并关联。

```
/绑定 淡岛百景
```

Bot 回复：`已将「淡岛百景」绑定到本群（123456789），对应子表：淡岛百景_工作表`

#### `/绑定id <fileID> <sheetID> [别名]`

通过文档 ID 和子表 ID 直接绑定，适用于子表命名不规范或跨文档的场景。

```
/绑定id abc123 sheet456 淡岛百景
```

Bot 回复：`已将子表 sheet456 绑定到本群，别名「淡岛百景」`

#### `/解绑 <番剧名>`

解除当前群与指定番剧的绑定。

#### `/绑定列表`

查看当前群绑定了哪些番剧，或全局查看所有绑定关系。

```
/绑定列表          ← 本群绑定的番剧（工作群）/ 所有番剧（总群）
/绑定列表 全部      ← 所有群的绑定关系（超级管理员，任意群可用）
```

#### `/设置流水线 <番剧名> <流水线定义>`

定义或修改番剧的工序链。详见 3.3 节。

```
/设置流水线 淡岛百景 翻译[分段] → 时轴[分段] → 校对 → 后期 → 监制 → 压制
/设置流水线 短片项目 翻译 → 校对 → 压制
```

#### `/查看流水线 <番剧名>`

查看当前配置的工序链。

#### `/新建集 <番剧名> <集数> [分段数=1]`（D12 简化）

创建一集的所有任务行并写入智能表。Bot 根据流水线配置自动展开：

```
/新建集 淡岛百景 7 3
```

假设流水线为 `翻译[分段] → 时轴[分段] → 校对 → 后期 → 监制 → 压制`，生成记录：

| 类型 | 集数 | 分段 | 组员 | 进度 | 完成时间 | 备注 |
|------|------|------|------|------|----------|------|
| 翻译 | 7 | 1 | | 未分配 | | |
| 翻译 | 7 | 2 | | 未分配 | | |
| 翻译 | 7 | 3 | | 未分配 | | |
| 时轴 | 7 | 1 | | 未分配 | | |
| 时轴 | 7 | 2 | | 未分配 | | |
| 时轴 | 7 | 3 | | 未分配 | | |
| 校对 | 7 | 0 | | 未分配 | | |
| 后期 | 7 | 0 | | 未分配 | | |
| 监制 | 7 | 0 | | 未分配 | | |
| 压制 | 7 | 0 | | 未分配 | | |

若流水线中不含某工序（如无后期），则不生成该工序的记录。标记为 `[分段]` 的工序按分段数展开（值 `1`..N），未标记的只生成一条「分段=0」记录。段范围信息（0-8 这类）不再自动写入，需要的话人工填到「备注」列。

执行后 Bot 在群内发布任务公告，列出所有可接任务。

#### `/新建特殊 <番剧名> <集数标识> <类型1> [类型2] [类型3] ...`

为 OP/ED 等特殊内容手动指定需要的工序类型，不受流水线模板约束，所有指定工序均不分段（分段=0）。

```
/新建特殊 淡岛百景 OP 翻译 时轴 校对 后期 压制
/新建特殊 淡岛百景 ED 翻译 校对 压制
```

生成记录（以第一条命令为例）：

| 类型 | 集数 | 分段 | 组员 | 进度 |
|------|------|------|------|------|
| 翻译 | OP | 0 | | 未分配 |
| 时轴 | OP | 0 | | 未分配 |
| 校对 | OP | 0 | | 未分配 |
| 后期 | OP | 0 | | 未分配 |
| 压制 | OP | 0 | | 未分配 |

特殊任务的依赖关系：仍然按流水线配置中相关工序的 `depends_on` 判断。如果指定的工序在流水线中不存在（如临时加的工序），则视为无依赖。

#### `/删除任务 <番剧名> <集数> [类型] [分段]`

删除指定任务记录。需要**二次确认**：Bot 先列出将被删除的记录摘要，用户回复"确认"后才执行删除。

```
/删除任务 淡岛百景 07
```

Bot 回复：

```
⚠️ 即将删除以下任务：
  淡岛百景 第07集，共 10 条记录
  其中 3 条已分配/进行中：翻译P1(@小明)、时轴P1(@阿杰)、时轴P2(@小王)
回复「确认删除」执行，30秒内有效
```

用户回复 `确认删除` → 执行删除。超时或回复其他内容 → 取消。

更精确的删除同样需要确认：

```
/删除任务 淡岛百景 07 时轴          ← 确认后删除第7集所有时轴任务
/删除任务 淡岛百景 07 时轴 P2       ← 确认后仅删除第7集时轴P2
```

#### `/修改任务 <番剧名> <集数> <类型> [分段] <字段>=<值>`

修改指定任务的某个字段。

```
/修改任务 淡岛百景 07 时轴 P2 备注=加急
/修改任务 淡岛百景 07 校对 进度=进行中
```

#### `/归档 <番剧名> <集数>`

将某一集所有"已完成"的任务批量标记为"归档"。

```
/归档 淡岛百景 07
```

### 4.2 组员命令

#### `/接活 <番剧名> <集数> <类型> [分段]`

认领一个任务。Bot 校验任务存在且进度为"未分配"后，写入组员 QQ 号并更新进度。

```
/接活 淡岛百景 07 时轴 P1
/接活 淡岛百景 07 校对            ← 不分段的工序无需指定分段
```

校验规则：
- 任务必须存在
- **写前单条重读**（D19）：执行前对目标行拉一次最新远端态（刷新缓存），按最新值判定——**进度=未分配 且 组员为空** 才可接；否则拒绝、不覆盖（防止覆盖两次同步间的手动填表，连"只填了名字没改状态"也保护）
- **不校验前置工序**（D15）：前置没完成也能接，支持提前认领排队；前置校验改在 `/完成` 时进行
- 同一人不能同时持有超过 N 个未完成任务（可配置，防止一人揽太多）

#### `/完成 <番剧名> <集数> <类型> [分段]`

标记任务完成，自动写入当前日期作为完成时间，并触发依赖检查与下游通知。

```
/完成 淡岛百景 07 翻译 P1
```

校验规则：
- 进度必须为"已分配"或"进行中"
- **前置工序必须全部完成**（D15）：同段串行时按同段判断，混合情况按全段；不满足则拒绝完成，回复"前置任务未完成，无法完成：['翻译 2']"
- 全员可完成（D3）：发送者 ≠ 当前组员时正常完成但附提醒

通知行为：
- 通知推送到该番剧绑定的**工作群**（不推送到总群）
- 组员标识为 QQ 号且在群内 → @该成员
- 组员标识为非 QQ 号（手动填表）→ 使用原始文本

#### `/放弃 <番剧名> <集数> <类型> [分段]`

放弃已接的任务，清空组员，进度回退为"未分配"，群内重新提醒该任务可接。

```
/放弃 淡岛百景 07 时轴 P1
```

#### `/进行中 <番剧名> <集数> <类型> [分段]`

将进度从"已分配"更新为"进行中"（可选，用于精细化进度追踪）。

### 4.3 查询命令（全部走本地缓存，不消耗 API）

查询命令在工作群和总群均可使用。在总群使用时可查看所有番剧的数据。

#### `/进度 <番剧名> [集数]`

展示某番或某集的任务状态一览。

```
/进度 淡岛百景 07
```

Bot 回复示例：

```
📺 淡岛百景 第7集 进度一览
─────────────────────
翻译 1   ✅ 已完成  @小明  05-20
翻译 2   ✅ 已完成  @小红  05-21
翻译 3   🔵 进行中  @小李
时轴 1   ✅ 已完成  @阿杰  05-20
时轴 2   ✅ 已完成  @阿杰  05-21
时轴 3   🟡 已分配  @小王
校对     ⚪ 未分配  （等待翻译/时轴完成）
后期     ⚪ 未分配
监制     ⚪ 未分配
压制     ⚪ 未分配
```

#### `/我的任务`

查询当前用户名下所有未完成任务。在工作群仅显示该群绑定番剧的任务，在总群显示所有番剧的任务。

#### `/待接 [番剧名]`

列出所有进度为"未分配"的可接任务。作用域同 `/我的任务`。

> D15：既然接活不再要求前置完成，`/待接` 就如实列出全部未分配任务，不再按前置是否满足过滤。

---

## 五、系统架构

### 5.1 整体数据流

```
QQ 群用户（总群 / 工作群 A / 工作群 B / ...）
   │
   │ 发送命令
   ▼
NapCat（QQ 协议层）
   │
   ▼
NoneBot2（命令路由）
   │
   ▼
nonebot-plugin-subflow
   ├── commands.py ──→ 解析命令参数 + 判断群类型（总群/工作群）+ 校验权限
   ├── task_manager.py ──→ 业务逻辑（校验、依赖检查、流水线解析）
   ├── cache.py ──→ 本地内存缓存（查询走这里，含 group_index）
   ├── storage/
   │   └── tencent_doc.py ──→ 腾讯文档 API（写操作走这里）
   └── messenger/
       └── qq.py ──→ QQ 消息发送（@成员 / 文本回退）
              │                         │
              ▼                         ▼
        腾讯文档智能表              通知推送到工作群（不推送总群）
```

### 5.2 项目目录结构

```
nonebot-plugin-subflow/
│
├── docker-compose.yml       # Docker Compose 编排（NapCat 默认注释）
├── Dockerfile               # NoneBot2 容器构建
├── .env.example             # 环境变量示例（复制为 .env 使用）
├── .env                     # 实际环境变量（.gitignore，不入库）
├── pyproject.toml           # 项目元数据 + 依赖
├── README.md                # 项目说明
│
├── data/                    # 运行时数据目录（Docker 挂载卷）
│   ├── bindings.json        # 群绑定关系（自动生成，首次启动创建空文件）
│   └── pipelines.json       # 流水线配置（自动生成，首次启动创建空文件）
│
└── nonebot_plugin_subflow/  # 插件源码
    ├── __init__.py           # 插件入口，注册事件钩子
    ├── config.py             # Pydantic 配置模型，从 .env 读取
    │
    ├── storage/              # 存储抽象层（可替换实现）
    │   ├── __init__.py
    │   ├── base.py           # 抽象接口 StorageBackend
    │   │   ├── get_sheets()
    │   │   ├── get_records()
    │   │   ├── add_records()
    │   │   ├── update_record()
    │   │   └── delete_records()
    │   ├── tencent_doc.py    # 腾讯文档实现
    │   │   └── auth()        # OAuth token 获取与自动刷新
    │   └── feishu.py         # （预留）飞书多维表格实现
    │
    ├── messenger/            # 通知抽象层（可替换实现）
    │   ├── __init__.py
    │   ├── base.py           # 抽象接口 Messenger
    │   │   ├── send_text()
    │   │   ├── send_at()
    │   │   └── resolve_user()  # 组员字段 → 是否可 @
    │   └── qq.py             # QQ / NapCat 实现
    │
    ├── bindings.py           # 群绑定关系管理
    │   ├── bind()
    │   ├── unbind()
    │   ├── get_group()       # 番剧名 → 群号
    │   ├── get_shows()       # 群号 → 番剧列表
    │   ├── is_main_group()
    │   ├── load()            # 从 bindings.json 加载（不存在则自动创建）
    │   └── save()
    │
    ├── pipeline.py           # 流水线配置管理
    │   ├── set_pipeline()    # 解析 DSL 并保存
    │   ├── get_pipeline()
    │   ├── get_depends()
    │   ├── get_downstream()
    │   └── expand_tasks()    # 根据流水线 + 分段参数生成任务列表
    │
    ├── cache.py              # 本地缓存管理
    │   ├── load_all()        # 启动时全量拉取
    │   ├── sync()            # 定时全量同步
    │   ├── query()
    │   └── update_local()    # 写操作后同步更新缓存
    │
    ├── task_manager.py       # 核心业务逻辑（纯逻辑，不依赖框架）
    │   ├── create_episode()
    │   ├── create_special()  # 新建特殊任务（手动指定工序）
    │   ├── claim_task()
    │   ├── complete_task()   # 完成 → 更新 + 依赖检查
    │   ├── abandon_task()
    │   ├── delete_task()     # 删除 → 生成确认摘要 / 执行删除
    │   ├── check_pipeline()
    │   └── archive_episode()
    │
    ├── commands.py           # NoneBot 命令处理器
    ├── models.py             # 数据模型（TaskRecord, PipelineStage 等）
    └── utils.py              # 工具函数（消息格式化、确认态管理等）
```

### 5.3 配置文件设计

#### `.env.example`（复制为 `.env` 后填写实际值）

```env
# ──── NoneBot2 基础 ────
DRIVER=~fastapi
HOST=0.0.0.0
PORT=8080
ONEBOT_WS_URLS=["ws://napcat:3001"]  # NapCat WebSocket 地址

# ──── 腾讯文档 API ────
TENCENT_DOC_CLIENT_ID=         # 开放平台应用 Client ID
TENCENT_DOC_CLIENT_SECRET=     # 开放平台应用 Client Secret
TENCENT_DOC_REDIRECT_URI=      # OAuth 回调地址
TENCENT_DOC_DEFAULT_FILE_ID=   # 默认文档 ID（按名绑定时在此文档下查找子表）

# ──── 群配置 ────
SUBFLOW_MAIN_GROUP_ID=         # 总群 QQ 群号（留空则不启用总群功能）
SUBFLOW_ADMIN_QQ_LIST=[]       # 管理员 QQ 号列表，JSON 数组格式

# ──── 业务配置 ────
SUBFLOW_MAX_TASKS_PER_USER=5   # 每人最大持有任务数（0=不限）
SUBFLOW_SYNC_INTERVAL=30       # 缓存同步间隔（分钟）
SUBFLOW_DATA_DIR=./data        # 运行时数据目录
SUBFLOW_DEFAULT_PIPELINE=翻译[分段] → 时轴[分段] → 校对 → 后期 → 监制 → 压制
```

#### 自动生成策略

| 文件 | 首次启动行为 | 说明 |
|------|-------------|------|
| `.env` | **不自动生成**，缺失则启动报错并提示从 `.env.example` 复制 | 含敏感信息，必须手动配置 |
| `data/bindings.json` | 自动创建空结构 `{"main_group_id": null, "bindings": {}}` | 运行时通过 `/绑定` 命令填充 |
| `data/pipelines.json` | 自动创建空结构 `{}` | 运行时通过 `/设置流水线` 命令填充，未配置的番剧使用 `SUBFLOW_DEFAULT_PIPELINE` |

`config.py` 使用 NoneBot2 标准的 Pydantic BaseSettings，从 `.env` 读取环境变量，类型校验失败时在启动阶段即报错。

> **可选字段空值说明**：`.env` 中 `SUBFLOW_MAIN_GROUP_ID=` 这类可选字段留空即可，Bot 启动时 `Config` 模型会通过 `model_validator` 将空字符串转为 `None`，自动回退到字段默认值（见实现方案 D14）。无需手动删除或注释掉不需要的配置行。

---

### 5.4 部署方案

#### Docker Compose 编排

```yaml
# docker-compose.yml

services:
  # ──── NoneBot2 + subflow 插件 ────
  nonebot:
    build: .
    container_name: subflow-bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data        # 持久化 bindings.json, pipelines.json
      - ./.env:/app/.env:ro     # 环境变量
    # depends_on:
    #   - napcat                # 如果启用 NapCat 则取消注释
    networks:
      - bot-network

  # ──── NapCat（QQ 协议层）────
  # 默认注释，如需容器化部署 NapCat 则取消注释
  # 也可使用外部已部署的 NapCat 实例，修改 .env 中 ONEBOT_WS_URLS 指向即可
  #
  # napcat:
  #   image: mlikiowa/napcat-docker:latest
  #   container_name: subflow-napcat
  #   restart: unless-stopped
  #   environment:
  #     - ACCOUNT=              # QQ 账号
  #     - WSR_ENABLE=true
  #     - WS_URLS=["ws://nonebot:8080/onebot/v11/ws"]
  #   ports:
  #     - "6099:6099"           # NapCat WebUI
  #   volumes:
  #     - napcat-data:/app/.config/QQ
  #   networks:
  #     - bot-network

networks:
  bot-network:
    driver: bridge

# volumes:
#   napcat-data:
```

#### Dockerfile

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
RUN pip install --no-cache-dir -e ".[all]"

COPY . .

# 确保 data 目录存在
RUN mkdir -p /app/data

CMD ["python", "-m", "nb", "run"]
```

#### 部署模式说明

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| **仅 NoneBot**（默认） | 只启动 `nonebot` 服务，连接外部已运行的 NapCat | NapCat 已在其他机器/容器上部署，或使用其他 OneBot 实现 |
| **全量部署** | 取消 `napcat` 服务注释，一键拉起两个容器 | 全新环境，希望一条 `docker compose up -d` 搞定一切 |
| **本地（pip / uv）** | 不用 Docker，直接在主机上建虚拟环境装依赖后 `python bot.py` | 已有 Python 环境、不想引入 Docker；pip 与 uv 仅"建环境+装依赖"一步不同 |

> 本地部署统一入口为 `python bot.py`（与 Dockerfile 一致），pip / uv 的具体命令见 [README「本地部署（pip / uv）」](../README.md#本地部署pip--uv)。NoneBot 从当前工作目录读 `.env`，需在含 `.env` 的仓库根运行。

全量部署时的启动流程：

```
1. docker compose up -d
2. 访问 http://localhost:6099 进入 NapCat WebUI 扫码登录
3. 登录成功后 NapCat 自动连接 NoneBot 的 WebSocket
4. 在 QQ 群中发送 /绑定 淡岛百景 开始使用
```

### 5.5 缓存策略详细设计

```
┌─────────────────────────────────────────────────┐
│                   内存缓存结构                    │
│                                                  │
│  cache = {                                       │
│    "淡岛百景": {                                  │
│      "sheet_id": "xxx",                          │
│      "file_id": "abc123",                        │
│      "group_id": 123456789,                      │
│      "pipeline": [...],                          │
│      "records": {                                │
│        "record_id_1": {                          │
│          "类型": "翻译",                          │
│          "集数": "7",                             │
│          "分段": "1",                             │
│          "组员": "123456789",                     │
│          "进度": "已完成",                        │
│          "完成时间": "2026-05-20",                │
│          "备注": ""                               │
│        },                                        │
│        ...                                       │
│      }                                           │
│    },                                            │
│    ...                                           │
│  }                                               │
│                                                  │
│  # 反向索引：群号 → 番剧名列表，用于快速判断       │
│  # 当前群可操作哪些番剧                           │
│  group_index = {                                 │
│    123456789: ["淡岛百景", "葬送的芙莉莲"],        │
│    223456789: ["孤独摇滚"],                       │
│  }                                               │
│                                                  │
│  main_group_id = 111111111                       │
│                                                  │
│  last_sync_time: "2026-05-25T14:30:00"           │
└─────────────────────────────────────────────────┘

同步策略：
  ✦ 冷启动：全量拉取所有子表及记录 → 约消耗 N 次 API（N = 子表数 × 2）
  ✦ 定时同步：每 30 分钟全量刷新一次 → 每天约 96 次 API
  ✦ 写穿透：每次写操作 API 成功后，立即同步更新本地缓存
  ✦ 写前重读（D19）：接活/完成/放弃/进行中 在写库前先对目标行单条重读，
    用最新远端态判定，避免覆盖两次同步间的手动填表
  ✦ 查询零消耗：所有 /进度、/我的任务、/待接 命令直接读缓存
```

### 5.6 外部表格变更检测与提醒（D17）

允许人工直接在腾讯文档里改数据，但改动此前完全静默。本机制在**定时同步那一刻**把"新拉到的数据"和"同步前的缓存"逐条比对，diff 出人工绕过 bot 的改动，按业务语义播报到对应**工作群**。

- **检测时机**：复用现有定时全量同步（`SUBFLOW_SYNC_INTERVAL`，默认 30 分钟），零额外 API。提醒最长延迟 = 一个同步周期。
- **为何只 diff 出人工改动**：bot 自己的写操作走"写穿透"已先更新缓存，同步时新旧一致、不产生 diff；只有绕过 bot 的直改才会被比对出来。
- **冷启动 / 新绑定子表**：缓存里没有旧快照 → 视为空 diff，不会把存量行当变更播报（重启不刷屏）。
- **范围**：进度流转（含回退）、组员变化、整行增删；纯备注/集数/分段/类型编辑忽略。
- **粒度**：每群本轮变更 ≤ 阈值（`SUBFLOW_EXTERNAL_CHANGE_DIGEST_THRESHOLD`，默认 5）逐条发，> 阈值合成一条汇总，防刷屏。
- **手动完成 → 下游解锁**：检测到某条被手改成"已完成"时，按依赖（同段串行/全段，见 3.4）算出新解锁的下游：未分配的广播"现在可以接了"；已被人接走的 @ 持有人"前置已完成，可以开始了"。
- **@ 规则**：涉及数字 QQ 组员则用 CQ 艾特码，昵称按文本（同 2.4）。
- **只读**：检测与播报不回写腾讯文档。
- **开关**：`SUBFLOW_NOTIFY_EXTERNAL_CHANGES`（默认开启）。
- **作用域**：只推送到绑定的工作群；总群无绑定，天然不收。

---

## 六、典型使用流程

### 场景：淡岛百景第 07 集完整生命周期

```
前置   管理员在「淡岛百景」工作群发送：/绑定 淡岛百景
       Bot → "已将「淡岛百景」绑定到本群，对应子表：淡岛百景_工作表"
       Bot → "已应用默认流水线：翻译[分段] → 时轴[分段] → 校对 → 后期 → 监制 → 压制"
       Bot → "如需修改，使用 /设置流水线 淡岛百景 ..."

14:00  管理员在工作群发送：/新建集 淡岛百景 7 3
       Bot → 校验「淡岛百景」已绑定本群 ✓
       Bot → 根据流水线配置展开任务，写入 10 条记录到智能表
       Bot → 工作群公告："淡岛百景 第7集 任务已创建，共 6 个可接任务（翻译×3 时轴×3）"
       （总群不收到任何推送）

14:05  小明在工作群发送：/接活 淡岛百景 7 翻译 1
       Bot → 校验通过，更新记录（组员写入小明的 QQ 号）
       Bot → "@小明 已接 淡岛百景7 翻译 1 ✅"

14:10  有人在总群发送：/进度 淡岛百景 7
       Bot → 正常展示进度（总群允许查询所有番剧）

14:15  有人在总群发送：/接活 淡岛百景 7 翻译 2
       Bot → "写操作请到对应工作群执行"

       ...（工作群内继续接活/完成）

15:00  小明在工作群发送：/完成 淡岛百景 7 翻译 1
       Bot → "@小明 完成了 淡岛百景7 翻译 1 ✅ 翻译还剩 2 个分段未完成"

       （若组员字段是手动填写的昵称"小明同学"，则：）
       Bot → "小明同学 完成了 淡岛百景7 翻译 1 ✅ 翻译还剩 2 个分段未完成"

16:30  翻译最后一个分段完成
       Bot → "淡岛百景7 翻译已全部完成 ✅ 等待时轴全部完成后，校对即可开始"

17:00  时轴最后一个分段也完成
       Bot → "🎉 淡岛百景7 翻译 + 时轴已全部完成！
              校对现在可以接了 → /接活 淡岛百景 7 校对"

17:05  校对员在工作群：/接活 淡岛百景 7 校对
       ...（后续工序依次推进）

次日   管理员要删除一个错误创建的任务：
       /删除任务 淡岛百景 7 时轴 2
       Bot → "⚠️ 即将删除：淡岛百景 7 时轴 2（已分配 @小王），回复「确认删除」执行"
       管理员回复：确认删除
       Bot → "已删除 ✅"

后续   全部完成后，管理员发送：/归档 淡岛百景 07
       Bot → 所有任务进度批量改为"归档"
```

---

## 七、后续扩展方向

- **催活提醒**：任务分配超过 N 天未完成，Bot 自动 @ 提醒组员
- **统计报表**：`/统计 <番剧名>` 查看各成员完成任务数、平均耗时
- **权限细分**：某些人只能接特定工序（如只能接翻译）
- **飞书迁移**：实现 `storage/feishu.py` + `messenger/feishu.py` 即可切换至飞书，业务逻辑不变
- **AstrBot 迁移**：重写 `commands.py` 适配 AstrBot 的命令系统，其余模块不变
